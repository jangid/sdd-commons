### Chunk F1: Fixture — a module the declared convention covers

**Goal**: exercise Check 3's declared-convention clause on a module the
commit gate runs (`chunk-close-review.md` §Checklist).
**Depends on**: none.
**Tasks**:

1. [implement] `plugins/sdd/tools/gc.py` — one rule of the drift sweep. — traces to `drift-sweep.md` §Sweep Table.

**Module**: `plugins/sdd/tools/gc.py`

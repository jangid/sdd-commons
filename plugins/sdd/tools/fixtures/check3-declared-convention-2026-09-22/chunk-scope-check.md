### Chunk F2: Fixture — a module outside the derived set

**Goal**: the same chunk shape with a module `CLAUDE.md` names only in prose
and no commit-gate hook runs (`chunk-close-review.md` §Checklist).
**Depends on**: none.
**Tasks**:

1. [implement] `plugins/sdd/tools/scope-check-selftest.py` — one arbitration scenario. — traces to `arbitrated-handoff.md` §Offline Arbitration Fixture.

**Module**: `plugins/sdd/tools/scope-check-selftest.py`

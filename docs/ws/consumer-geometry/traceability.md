---
workstream: consumer-geometry
last_updated: 2026-09-22
---

# Traceability — consumer-geometry

Rows owned by the `consumer-geometry` workstream (per `docs/spec/ws-traceability.md`).
One row per requirement this workstream delivers; Spec / Test / Implementation /
Verified are filled by `sdd:specs`, `sdd:implement` and `sdd:verify`. The shared
aggregate `docs/requirements/traceability.md` is regenerated from this file and
its siblings — never hand-edited, and never by a leaf: regeneration is the
orchestrator's post-gate bookkeeping.

`Verified` takes the values of `docs/spec/ws-traceability.md` §Legal `Verified`
Cell Values. Rows were created by the orchestrator at the requirements stage
gate on 2026-09-21; `Spec` is `sdd:specs`' cell, `Test` and `Implementation`
are `sdd:implement`'s, and `Verified` is `sdd:verify`'s — all four are
intentionally blank here.

The amended `REQ-PKG-MARKETPLACE-006` and `-007` are **not** given rows here.
Their rows belong to the `marketplace` workstream, which owns them; this cycle
amends their text in the shared corpus and records the amendments in
`docs/requirements/index.md`'s delta note. Adding duplicate rows would make the
regenerated aggregate carry two assertions per id.

**Requirement → plan task.** The matrix shape is fixed at the six columns of
`docs/spec/ws-traceability.md` §The Per-Workstream File, so the plan stage adds
no `Task` column — a seventh cell makes every row unparseable and the drift
sweep drops it from the regenerated aggregate (`[traceability-rowdrop]`).

Spec cells cite the `## Consumer-Geometry Amendment` section of each file. `two-root-linter.md`'s subsections are numbered `§CG-1`…`§CG-11` (plus `§CG-5a`);
`drift-sweep.md`'s are numbered `§1`…`§4` — `§1` the `--suite-root` surface, `§2`
`lint_command()`'s pass-through, `§3` `sweep_lint()`'s `GEOMETRY:` forwarding,
`§4` `AGG_FIX` — exactly as its `### 1.`…`### 4.` headings spell them. In
`marketplace-packaging.md`, `skill-lint-v5.md` and `pre-commit.md` the
subsections are **named**, so those citations quote the name rather than a
number and a renumbering cannot silently misroute them.

| Requirement | Spec | Workstream | Test | Implementation | Verified |
|-------------|------|------------|------|----------------|----------|
| REQ-PKG-CONSUMERGEOMETRY-001 | `two-root-linter.md` §CG-7 + criteria; `marketplace-packaging.md` §CG "The three checklist items…" (freeze repin); `drift-sweep.md` §2, §4; `pre-commit.md` §CG | consumer-geometry | `skill-lint.py --self-test` / `gc.py --self-test`: `cg_reconcile()` asserts constant-vs-registered both ways; falsifiability demonstrated by two mutation runs (Chunk 0 task 2 — token-without-case and case-without-token each print their own `cg-row-` line). **Row 4** registered in Chunk 2 in **`gc.py`'s** self-test (not the linter's), covering `lint_path()`'s sibling-first precedence and both `lint_command()` branches; its two plan-named mutations run — revert branch (i) to `[executable, str(lint), str(self.root)]`, and reorder the `lint_path()` candidate tuple — each putting a line beginning `cg-row-4:` in the printed failure list, which is the comparand, never the process exit code. **Row 3** registered in Chunk 1 as case `zero_sweep_summary_is_distinguishable`, its mutation run (drop the `— NOTHING SWEPT` suffix → the printed list carries `cg-row-3:` lines; control → none). **Rows 1 and 2** registered in Chunk 3 in the linter's self-test as `cg_row_1_retired_prefix_under_a_disjoint_suite_root` and `cg_row_2_required_rows_under_a_disjoint_suite_root` — both on **disjoint** fixtures, each pairing a suite-root seed with a differently named corpus-root decoy so the reported location discriminates the binding. Row 1's mutation run (`retired_scope_roots()` → `[self.corpus_root]`) puts two `cg-row-1:` lines in the printed list; row 2's (all three `f = self.suite_root / rel` sites → `self.corpus_root`) puts four `cg-row-2:` lines there, and also trips C12.1 and the §3 retarget case — which is exactly why membership of the printed list, never the process exit code, was the comparand. Row 2 covers both populations of the 56: the 16 gated-table rows and a `REQUIRED` table row whose message flips `found 0x` → `file missing entirely` under the mutation. **Rows 5-8** registered in Chunk 4 in the linter's self-test. Row 5 (`cg_row_5_population_wiring_and_scope_dedup`) pins `main()`'s `--print-population` wiring through **subprocess CLI** runs on two fixtures — a tier-1 corpus whose only skill file lies under an explicit `vendor/suite` root, and a tier-2 corpus whose only skill file lies under a derived `plugins/sdd` root — plus `retired_scope_entries()`'s deduplication on a union-bound `CLAUDE.md` whose corpus-side copy is a symlink to the suite-side file. Both its mutations run: mis-rooting the wiring to `(root, root)` flips both CLI lines to `FILES_SWEPT=0` (two `cg-row-5:` lines); removing the `seen` set yields two arrivals of one resolved path and two identical `[retired-prefix]` findings (two more). Rows 6, 7 and 8 are **fixture-only** (plan D2): their `rel=` overrides were already in the source, and each new case observes one on a **disjoint** fixture, the only geometry in which the flagged file lies under no swept root — under a nested fixture the mutation is invisible, which is the vacuity class this cycle closes. Row 6 covers `check_retired_prefix()`'s one call site, row 7 `check_required()`'s **three** present-file call sites in one fixture (a short `REQUIRED` row plus a `skills/verify` seed firing both gated tables; the `file missing entirely` branch is deliberately excluded, since it flags a relative path `rel()` passes through), row 8 `check_template_drift()`'s `rel=Path(TEMPLATE_SOURCE)` on all four anchors. Each mutation (delete the keyword argument(s)) was run and each put its own `cg-row-<n>:` line in the printed list. All mutations run on `cp -R` scratch copies under `$TMPDIR`; the unmutated scratch control prints no `cg-row-` line **Acceptance 5 (Chunk 6 task 6)**: the spec-side freeze item is repinned and asserted **on that named item**, never by a file-wide grep — `HEAD` occurs in that file in unrelated contexts and "the working tree" has no grep spelling at all; both blob equalities re-verified at `3ddfdb3` × `0bdb076`. **Chunk 7**: no new fixture — row 5's enumeration hazard is recorded as `Q-IMPL-CONSUMERGEOMETRY-004` in `two-root-linter.md`, and the requirement's table is left byte-unchanged (the approved comparand set is never rewritten). **Chunk 8, acceptance 4**: both self-tests green on the shipping tree — `skill-lint.py --self-test` and `gc.py --self-test` each exit 0 — and both are still **hook entries** rather than moved to the run-explicitly set, asserted **by parsing** `.pre-commit-config.yaml`: ids `skill-lint-self-test` and `drift-sweep-self-test` under `repo: local`, with entries `python3 plugins/sdd/tools/skill-lint.py --self-test` and `python3 plugins/sdd/tools/gc.py --self-test`, in an eight-entry hook set. This is a **presence** claim only: the byte-identity claim is Chunk 8 task 2's and the deliberate declining is Chunk 5 task 6's claim about **geometry**; the three are asserted separately and none is cited as evidence for another. | `plugins/sdd/tools/skill-lint.py`: `CG_ROW_TOKENS` (rows 1-3, 5-8), `cg_check()` registration, `cg_reconcile()`; `plugins/sdd/tools/gc.py`: the same surface for row 4 alone. Both constants empty at Chunk 0 close (plan D1, incremental population); `skill-lint.py`'s `CG_ROW_TOKENS` holds `("cg-row-3:",)` at Chunk 1 close; `gc.py`'s holds `("cg-row-4:",)` at Chunk 2 close — complete at one member, never eight; `skill-lint.py`'s holds `("cg-row-1:", "cg-row-2:", "cg-row-3:")` at Chunk 3 close; at Chunk 4 close it holds **seven** members — rows 1-3 and 5-8 — while `gc.py`'s still holds row 4 alone, so the eight-row reconciliation is the **union of both tools' printed tokens** and neither tool ever names eight. Chunk 4 adds four self-test cases and no behaviour change: both of row 5's sites were already correct in the tree the chunk opened on (the wiring was repaired at Chunk 3; the `seen` set predates this cycle's entry sha `3bac4af`), so all four rows are fixture-only. The one harness change is the case-runner guard on the `for _case in (...)` loop, which records an aborting case as a failure instead of killing the process — without it, rows 6-8's mutations abort earlier unrelated cases before the `SELF-TEST FAIL:` list is printed and membership, the §CG-7 comparand, would be unobservable Chunk 6 adds no tool behaviour under this requirement beyond `gc.py`'s `AGG_FIX` string: `docs/spec/marketplace-packaging.md`'s freeze item and its accepted alternative form are repinned to `0bdb076`, so the window closes where the requirement says it closes rather than at an open right endpoint. **Chunk 7**: no tool change. The `[Updated: 2026-09-21c]` note under this requirement is carried by -004's note; the durable decision — name mutation sites by function, never by source expression — is `Q-IMPL-CONSUMERGEOMETRY-004`. **Chunk 8**: no production file edited — the chunk is wholly verification, and `.pre-commit-config.yaml` is asserted unchanged, never written. | pass |
| REQ-PKG-CONSUMERGEOMETRY-002 | `two-root-linter.md` §CG-4 (citation correction), §CG-5, §CG-5a | consumer-geometry | `skill-lint.py --self-test` case `per_geometry_split_holds` (Chunk 3 task 6): acceptance 1 on a fixture whose suite subdirectory is named **`vendor/suite`, not `plugins/sdd`** (§CG-5a) — with the root passed explicitly, `suite_contained()` is `True` and `len(skill_files()) > 0`; with no root passed, `derived_suite_root()` returns `None` by construction and `len(skill_files()) == 0`. §7 fixture A's tree is deliberately not reused. Acceptance 2 (the Option B tripwire) re-asserted in the same case on fixture B's consumer shape — `swept_roots()` exactly `{corpus_root}` and zero walked files from the suite root; mutation run (admit a disjoint suite root to `swept_roots()`) turns fourteen lines red including both of this case's. Acceptance 3: C12.1 registered and unmodified, pinning its 16 rows and deliberately not 56. Case `derivation_declines_for_a_foreign_consumer` covers the consumer bullet's "today's union, unchanged" in both negative directions, with its two mutations run **Chunk 7**: the consumer bullet's citation asserted by reading -006 acceptances 3 and 5 directly — both are negative-direction only — and §CG-4's acceptance 6 confirmed present as the positive direction, so the bullet's exception is asserted in both directions across three acceptances, not two. | `plugins/sdd/tools/skill-lint.py`: no rebinding of the 56 suite-gated rows — the split is delivered by the tier-2 re-rooting in `Linter.__init__` alone (Option A as a side effect of the adopted root), with `swept_roots()` unchanged so a disjoint suite root still contributes nothing **Chunk 7**: `docs/requirements/integration/packaging.md` — one appended `[Updated: 2026-09-21c]` note beneath this requirement correcting the citation. No approved sentence rewritten; the rule the bullet states is unchanged. | pass |
| REQ-PKG-CONSUMERGEOMETRY-003 | `two-root-linter.md` §CG-2, §CG-9 (the `:89` and `:522-523` rows); `drift-sweep.md` §1, §2; `skill-lint-v5.md` §CG criteria | consumer-geometry | `skill-lint.py --help` names `--suite-root` (the deliberate inversion of REQ-PKG-PACKAGING-003's negative-surface grep) and, in a scratch fixture, an explicit suite root under the corpus root yields `suite_contained() == True` with a non-empty `skill_files()` while the same corpus without it falls through to `default_suite_root()`; `gc.py --help` names `--suite-root` and `Gc(...)` accepts and stores the keyword. `gc.py --self-test` case `cg-row-4:` covers both `lint_command()` branches — branch (i) asserted **on the constructed vector**, branch (ii) by running the `-c` shim and reading `suite_contained()` back off the `GEOMETRY:` token. Six mutation runs recorded in the plan's Chunk 2 Notes (branch (i) reverted; the shim's ctor reverted; the linter's flag removed; the argument accepted-and-discarded; `gc.py`'s flag removed; the `Gc` keyword removed) — the branch pair reds in opposite directions. Omission half: with `suite_root is None` both branches are byte-identical to the entry sha `3bac4af`. Leg (i) greps run: `no-suite-rules` still absent, no production `suite_rules=False` construction site **Chunk 7, acceptance 4, both halves and the residual**: the §2 deferral sentence (`:89`) re-scoped in place and the `:522-523` item's **middle clause only** excised; both surviving clauses asserted positively present (`no-suite-rules` 7 occurrences, `suite_rules=False` 11). **Residual grep, run-time-derived**: with the `## Consumer-Geometry Amendment` heading line read at run time (`:750` today) and fence spans computed, `docs/spec/two-root-linter.md` carries **zero** sentences asserting the absence of a `--suite-root` surface outside a fence and before that heading; the 6 occurrences at or after it are bounded as citations made in order to retire the sentence (prose plus §CG-9's table, whose cells cannot be fenced). | `plugins/sdd/tools/skill-lint.py`: the `--suite-root PATH` argument on `main()`'s parser, resolved to an absolute path and adopted as tier 1 **with no existence test**, passed to `Linter(root, suite_root)` (and to `print_population()`); omitted it stays `None` so the constructor's lower tiers answer. `plugins/sdd/tools/gc.py`: the matching `--suite-root` flag, the `Gc(..., suite_root=None)` constructor parameter, and `lint_command()`'s pass-through on **both** branches — the argv branch appends `--suite-root <path>`, the `-c` shim threads the root through its program string as `sys.argv[3]` with a second ctor spelling, so "pass nothing" stays byte-identical by construction **Chunk 7**: `docs/spec/two-root-linter.md` §2 bullet re-scoped — the `--suite-root` deferral marked superseded by this requirement while the `--no-suite-rules` leg (REQ-PKG-PACKAGING-003 leg (i)) is stated untouched — and the §Acceptance Criteria item's middle clause excised with the item surviving. `docs/requirements/integration/packaging.md`: one appended `[Updated: 2026-09-21c]` note reconciling the `:516-517`→`:522-523` and `:83`→`:89` citations and stating the in-place shape of the §2 edit. | pass |
| REQ-PKG-CONSUMERGEOMETRY-004 | `two-root-linter.md` §CG-6; `drift-sweep.md` §3; `skill-lint-v5.md` §CG "The summary-line pins", "The GEOMETRY: token is not a finding" | consumer-geometry | `skill-lint.py --self-test` case `zero_sweep_summary_is_distinguishable` (row 3) — the presence-iff suffix, asserted over a one-file and an empty corpus with the swept-file count normalised away; the three acceptance mutations run on `disjoint_scratch_suite()` copies (drop the suffix, make it unconditional, remove the `GEOMETRY:` emission) with the results in the plan's Chunk 1 Notes. Enum rendering (`disjoint`/`nested`/`equal`), tier-1 `suite-rows-root=`, emission on the failing path and none from `--self-test` recorded there as four separate assertions; tier-2 `suite-rows-root=` owed to Chunk 3 task 4. **The forwarding clause** discharged in Chunk 5: in §CG-8's disjoint scratch construction the far `gc.py --report --root "$REPO"` output carried **no** `GEOMETRY: ` line before the change and carries exactly one after it, forwarded verbatim on its own line; the reverting mutation (delete the pass-through branch in `sweep_lint()`) was run on a `$TMPDIR` scratch copy and returned the count to zero. The *derives-no-geometry* half is asserted as **both** halves of the restated `drift-sweep.md` criterion: (a) the grep for `suite_contained` and the enum literals `nested`/`equal`/`disjoint` over `gc.py` matches only inside the forwarding pass-through — the two row-4 self-test assertions that compare a string lifted verbatim off a linter run — and nowhere that computes a member from path comparison; (b) the mutation was **run**, not described: computing the token inside `gc.py` and emitting it alongside the forwarded one made the far `gc.py --report` output carry a **duplicate** `GEOMETRY:` line (count 1 → 2), and reverting returned it to one **Chunk 7**: all **four** summary-line pins read at implement time and confirmed **not** end-anchored — `skill-lint-v5.md:129,:429,:454` and `docs/requirements/integration/skill-lint.md:276`, the set derived by `grep -n 'OK: N file(s) clean'` and identified by that string rather than by number; each states a *matches/prints* prefix-or-substring relation, so **none** needed amendment and the requirements-side file was **not** written. The three repaired paths asserted **by resolving them at run time**: `plugins/sdd/tools/skill-lint.py` exists; the pre-change spelling `tools/sdd-skill-lint.py` resolves under neither layout, which is what made that half red before the change. The `derives-no-geometry` comparand now stated identically in `two-root-linter.md` and `drift-sweep.md`. | `plugins/sdd/tools/skill-lint.py`: `Linter.geometry()` and `Linter.geometry_line()` (the three-member enum, `swept-roots=`, the **effective** `suite-rows-root=`), emitted by `run()` alone on its own line immediately before the summary, and the `— NOTHING SWEPT` suffix on **all three** print sites (`FAIL:`, the warn `OK:` variant and the clean `OK:` variant). `plugins/sdd/tools/gc.py`: the forwarding half (Chunk 5) — `self.forwarded_geometry`, set by `sweep_lint()`'s one pass-through branch (a literal `GEOMETRY: ` prefix test that carries the line across as an opaque string) and re-emitted by `run()` on its own line immediately before the summary, mirroring the linter's placement. It is not a finding: no severity, no entry in `self.findings`, not counted in the summary's `N`. Nothing in `gc.py` parses, rebuilds or derives the token **Chunk 7**: `docs/spec/skill-lint-v5.md` — the three summary-line pin invocations corrected from `python3 tools/sdd-skill-lint.py` to `python3 plugins/sdd/tools/skill-lint.py`; other occurrences in that file stay out of scope per its own §The summary-line pins. `docs/spec/two-root-linter.md` — the `gc.py` derives-no-geometry criterion restated as the two-half (grep + mutation) comparand its `drift-sweep.md` twin carries. `docs/requirements/integration/packaging.md` — one appended `[Updated: 2026-09-21c]` note recording §CG-6's re-reading of `suite-rows-root=<path>` (as given → **effective**) and of "every run" (→ iff a summary is printed, excluding `--self-test`), plus the superseded two-site pin measurement. Structural basis of the one-token-per-summary property recorded as `Q-IMPL-CONSUMERGEOMETRY-002`. | pass |
| REQ-PKG-CONSUMERGEOMETRY-005 | `marketplace-packaging.md` §CG "The removal", "The three disposition classes", "The shape every string criterion…", "The three checklist items…", "Consumer-unreachable strings…"; `two-root-linter.md` §CG-9 (the `:366` row); `drift-sweep.md` §4; `pre-commit.md` §CG | consumer-geometry | Chunk 6, all thirteen tasks run. **Removal**: `test ! -d plugins/sdd/skills/orchestrate/tools` succeeds and a run-time `grep -rn 'orchestrate/tools'` over the repository **excluding `docs/`** returns **0**, where it returned 1 at the shipped `references/drift-sweep.md:36` before the change (acceptance 1). Pre-removal identity re-checked: `telemetry.py` byte-identical to its suite-root counterpart; `gc.py` differs only by this cycle's own Chunks 1-5 edits to the suite-root copy, which is why identity is asserted at the entry sha and not at HEAD. **Class A/C primary half** (acceptance 2): the spec-named pre-amendment sentences asserted absent **whitespace-normalised** (`two-root-linter.md` §8, the shipped `drift-sweep.md`, and `marketplace-packaging.md:181`'s derived-population prose); `pre-commit.md`'s Q-IMPL-MARKETPLACE-019 sentence survives only as an exempt **citation inside that file's own amendment section**, its live site corrected. File set derived from REQ-PKG-CONSUMERGEOMETRY-005's disposition table at run time **minus the aggregate** (plan D4 carve-out, recorded not missed — the aggregate's class (C) row is discharged by the orchestrator's post-gate regeneration from the per-ws source, and a leaf that regenerates is a violation even when its output is right). **Secondary half, derived at run time with no count written down**: `marketplace-packaging.md`'s glob grep returns **0** unexempt matches against its three stated exemptions (fenced block; at/after its own `## Consumer-Geometry Amendment` heading, whose line number is read at run time — `:624` today; and the Q-IMPL-MARKETPLACE-028 reverted-record line, exempt at `:549`), with the two exempt amendment citations at `:776` and `:965`; `pre-commit.md`'s `skills/orchestrate/tools` grep returns **0** before its heading (`:404`) where it returned exactly one, and 3 exempt citations after it; `two-root-linter.md` likewise **0** before `:743`. **Class B** (acceptance 3): 20 dated notes across the four records, `git diff --numstat` per file **+14/-0, +9/-0, +2/-0, +3/-0** — insertion-only, zero deletions, every original sentence byte-preserved. **Class C** (acceptance 4): both per-ws source rows corrected; the aggregate's rows **read by requirement id, never by line** and recorded — the -006 Evidence cells and the -007 Test cells currently differ from the corrected source exactly as expected pending the orchestrator's regeneration, compared as **cell strings directly**. Both blob pins re-verified EQUAL: `3ddfdb3:tools/gc.py` == `0bdb076:plugins/sdd/tools/gc.py` (`d800df50…`) and the same for `telemetry.py` (`d696421e…`). **Checklist items** (acceptance 5): item (i) asserted to survive as a checklist item with **zero** glob occurrences on its line and **both** surviving clauses positively present (`git log --follow` to pre-change history; the two-sided loss check with its sides spelled separately); item (ii) retired whole. **Acceptance 7**: every corrected path resolves from the repository root, and -007's binding re-run green — no telemetry invocation under `plugins/sdd/skills/` passes a `--file` value beginning with a skill or plugin directory (all three keep `.sdd/telemetry.jsonl` or `<path>`). **Acceptance 6**: `gc.py --report --root .` exit 0 with `0 warning(s), 34 info` and `GEOMETRY: nested  swept-roots=2` both before and after the removal — unregressed, evaluated across the removal alone. The `OPEN:` consumer residue still present at `marketplace-packaging.md:884` with its blocking constraint named **Chunk 7**: the requirement-side half of Chunk 6 task 10's third grep exemption landed, closing the three-way split — requirement, spec and plan now say one thing about `marketplace-packaging.md`'s prescribed Q-IMPL-MARKETPLACE-029 text and about `docs/ws/marketplace/traceability.md`'s -006 Evidence cell. No re-run of Chunk 6's assertions; this chunk adds no code path. | **Removed**: `plugins/sdd/skills/orchestrate/tools/` (`gc.py`, `telemetry.py`). **Class A, in place** — shipped surface: `plugins/sdd/skills/orchestrate/references/drift-sweep.md` (the `:36` citation re-tensed and the path string retired), `plugins/sdd/skills/orchestrate/USAGE.md` `:159,:439,:572` and `references/telemetry.md` `:484,:577,:602` (six bare `python3 tools/telemetry.py` → `python3 plugins/sdd/tools/telemetry.py`, **script path only**, the telemetry *file* path left absent), `plugins/sdd/tools/gc.py`'s `AGG_FIX` string (`tools/gc.py --fix` → `plugins/sdd/tools/gc.py --fix`, a documentation-only edit under REQ-PKG-CONSUMERGEOMETRY-001's permission); shared specs: `docs/spec/two-root-linter.md` §8 restated in the past tense against the removal (§CG-9 row 3; plan **D3** — Chunk 7 lands this file's other, `--suite-root`, edits), `docs/spec/pre-commit.md`'s Q-IMPL-MARKETPLACE-019 **Context** re-tensed with an inline dated clause (the recorded context and the Q-IMPL's decision both unaltered), `docs/spec/marketplace-packaging.md`'s §Tools derived-population prose and its §Tools table row, plus Q-IMPL-MARKETPLACE-029's **Decision** given the inline dated clause the spec spells out verbatim — decision text untouched, the clause saying only that its object is gone. Checklist edits in the same file: the bundled-population **clause excised** with the item surviving, the drift-sweep-resolves-to-the-bundled-copy item **retired whole** in this file's own strikethrough-plus-note spelling, the REQ-PKG-MARKETPLACE-007 freeze item and its accepted alternative form **repinned** from the working tree / `HEAD` to `0bdb076`, and the **third residual-grep exemption written into the Approved criterion** (§CG-4's rule for reinterpreting an Approved literal; the matching `[Updated: 2026-09-21c]` requirements note is Chunk 7 task 1(c)'s). **Class B, appended dated notes only** — cross-workstream, originals never rewritten, `[Superseded 2026-09-21 — REQ-PKG-CONSUMERGEOMETRY-005 … true at <sha>]` with `29febe6` for `docs/ws/marketplace/verification.md` (11 notes, one of them the single note covering the dead comparand's five occurrences) and `0bdb076` for `docs/ws/packaging/verification.md` (6), `docs/ws/packaging/baseline.md` (1) and `docs/ws/packaging/plan.md` (2). **Class C, in place as data** — `docs/ws/marketplace/traceability.md:56` (the REQ-PKG-MARKETPLACE-006 Evidence cell replaced with the shape the spec specifies verbatim: live `plugins/sdd/tools/` paths plus a dated parenthetical retiring the duplication evidence) and `:57` (the REQ-PKG-MARKETPLACE-007 dead comparand replaced with the pinned blob-sha form; no `3ddfdb3 HEAD` spelling survives). `docs/requirements/traceability.md` **not touched** (plan D4) **Chunk 7**: `docs/requirements/integration/packaging.md` — one appended `[Updated: 2026-09-21c]` note beneath this requirement stating the exemption for acceptance 2's secondary grep and re-reading acceptance 5's "returns zero matches" literal with it. Durable record: `Q-IMPL-CONSUMERGEOMETRY-006`. | pass |
| REQ-PKG-CONSUMERGEOMETRY-006 | `two-root-linter.md` §CG-1, §CG-3, §CG-4, §CG-8, §CG-10; `pre-commit.md` §CG | consumer-geometry | `disjoint_scratch_suite()` is callable from either tool's self-test fixture layer; the §CG-8 construction was run and its four pre-change observations pinned in the plan's Chunk 0 Notes ; **acceptance 6 (§CG-4, the positive direction)** discharged in Chunk 3 by case `derivation_positive_direction` on **fixture D**, the look-alike corpus `F` holding both `F/skills/own-skill/` and a seeded `F/plugins/sdd/skills/lookalike-skill/`, invoked with corpus root `F` and no explicit suite root: `suite_contained()` `True`; the token reads `GEOMETRY: nested` with `swept-roots=2`; `suite-rows-root=` renders `F/plugins/sdd` with `default_suite_root()` asserted **absent** from the line; and the seeded walk-class violation is reported **by name** at `skills/lookalike-skill/SKILL.md`, never by a count. Three mutations run, each on a `$TMPDIR` scratch copy: drop tier 2; key tier 2 on `default_suite_root()`; invert `rel()`'s deepest-root rule so the seed renders `plugins/sdd/skills/…`. This also discharges Chunk 1 task 4(c), the tier-2 `suite-rows-root=` rendering. Acceptance 4 (the in-repo case unregressed) by case `nested_case_is_not_regressed` — tier 2 asserted to **decline** the in-repo candidate, and the implicit construction's swept-file count compared against the explicit one, no literal. Every construction path asserted equal by case `shim_and_cli_resolve_the_same_tiers`, which runs `gc.py`'s branch-(ii) `-c` shim and a **subprocess** CLI run over the same corpus and compares their `GEOMETRY:` lines; mutation run (tier 2 moved into `main()`) → `shim 'disjoint' vs CLI 'nested'`. §CG-8 re-measured after the change: the far `skill-lint.py` prints `GEOMETRY: nested  swept-roots=2` and `OK: 25 file(s) clean` with no `[structure]` finding, and the far `gc.py --report` passes none through. **Re-measured again at Chunk 5**, now end to end on both tools: the far `skill-lint.py` prints `GEOMETRY: nested  swept-roots=2`, raises no `[structure] skills/ directory not found` finding and reports a non-zero swept-file count (`OK: 25 file(s) clean`), and reverting the tier-2 derivation in `Linter.__init__` on a scratch copy returns it to red — `GEOMETRY: disjoint  swept-roots=1` with the `[structure]` finding and `FAIL: 1 finding(s), 0 warning(s) — NOTHING SWEPT`. In the same construction the far `gc.py --report --root "$REPO"` raises **no** `[structure]` finding, asserted **on the finding set** rather than on the token; the same derivation revert makes exactly one reappear, while reverting `lint_command()`'s suite-root pass-through alone leaves the set empty (recorded as a provisional observation in the plan's Chunk 5 Notes — under the order actually landed, the pass-through is inert when no `--suite-root` is supplied and the repair is the linter's derivation). Nested control re-run: the in-repo `skill-lint.py` is unchanged at `GEOMETRY: nested` with its run-time-derived file count, no literal pinned; **no assertion is made about the committed hook set's geometry**, deliberately — its byte-identity is a different claim, asserted at Chunk 8 task 2 **Chunk 8, acceptance 4 re-asserted on the shipping (post-Chunk-7) tree**: the in-repo `skill-lint.py .` is unregressed at `GEOMETRY: nested  swept-roots=2` with its **run-time-derived** swept-file count (`OK: 25 file(s) clean` as read off the run; no literal pinned) and exit 0; `gc.py --report --root .` exits 0 at `0 warning(s)` and raises **no new finding class** — the only two classes present, `[qimpl-unreferenced]` and `[stale-chain]`, are both pre-existing, and the rule-class literal set in `gc.py` is identical to the entry sha `3bac4af`. **§CG-8 re-run end to end one final time** on that same tree: the far `skill-lint.py "$REPO"` prints `GEOMETRY: nested  swept-roots=2` and `OK: 25 file(s) clean` with no `[structure] skills/ directory not found` finding, and the far `gc.py --report --root "$REPO"` exits 0 with **zero** `[structure]` findings passed through and the same forwarded token — byte-equal to the in-repo run, so the repair is confirmed against the tree that ships and not only against the tree Chunk 5 closed on. **Hook-set byte-identity (task 2, a different comparand)**: `git diff 3bac4af HEAD -- .pre-commit-config.yaml` is empty and the blob is the same object `689d689e…` at `3bac4af`, at `HEAD` and in the working tree — no entry added, removed or re-rooted, which is exactly what this delta authorises. **The commit gate (task 3)**: `pre-commit run --all-files --show-diff-on-failure` runs **seven of eight** hooks green — `drift-sweep`, `skill-lint`, both self-tests, `trailing-whitespace`, `check-yaml`, `check-json` — while `end-of-file-fixer` cannot complete in this sandbox (`PermissionError: [Errno 1] Operation not permitted: '.claude/settings.json'`, a write-denial on a file wholly outside this cycle's write scope; precedent recorded in `docs/ws/marketplace/verification.md`). Coverage recovered with `--files`: that hook passes over all **208** tracked files but the denied one, and both writing hooks pass over the **23** files this cycle changed. The denied file already ends in `0a`, so the failure is the hook's `rb+` open being refused, not a content finding. **No hook artefact landed** — `git status --short` is empty after every run, so the carve-out was never exercised. | `plugins/sdd/tools/skill-lint.py` and `plugins/sdd/tools/gc.py`: `disjoint_scratch_suite(dest)` copies this tool's own plugin dir into `$TMPDIR`, disjoint by construction, never the installed cache; `plugins/sdd/tools/skill-lint.py`: `derived_suite_root(corpus_root)` (tier 2 — all three conjuncts: `candidate.is_dir()`, `(candidate/"skills").is_dir()`, `candidate.resolve() != default_suite_root().resolve()`), resolved **in `Linter.__init__`** in place of the former tier-3 expression so every construction path reaches it, and `print_population()`'s `suite_root` made optional (it accepts and passes `None` through) so `--print-population` inherits the same resolution. `plugins/sdd/tools/gc.py` derives **no** geometry — its `-c` shim inherits the linter's derivation for free, which is the whole content of §CG-1, and its only Chunk 5 change is the `sweep_lint()`/`run()` forwarding pass-through carried under REQ-PKG-CONSUMERGEOMETRY-004 **Chunk 8**: no production file edited — all five tasks are verification, and no regression was found, so nothing routed back to an owning chunk. | pass |

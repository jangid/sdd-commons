---
last_updated: 2026-09-22
---

# Research Index

| ID | Topic | Date | Status | Summary |
|----|-------|------|--------|---------|
| RS-001 | SDD artifact structure | 2026-04-27 | Complete | Plan bloat, research traceability, and requirements splitting — v2 structure designed |
| RS-002 | SDD skill improvements | 2026-05-25 | Complete | Chunk-close review checklist, Q-IMPL protocol, per-milestone plans, drift detection — six workflow gaps identified from rubric M1 |
| RS-003 | v3 migration path | 2026-05-25 | Complete | One migration-required change (plan vocabulary), lightweight v2→v3 procedure, v1→v3 via sequential composition |
| RS-004 | sdd-review skill design | 2026-05-25 | Complete | External review catches critical issues at phase boundaries; ~7 requirements for a formal skill covering phase detection, report format, trigger classification, and session isolation |
| RS-005 | sdd-orchestrate feasibility | 2026-06-04 | Complete | Subagent skill-invocation and dispatch-time review isolation both proven with live dispatches; no new resume marker needed, fan out along independent plan chunks, package as one skill — greenlight for requirements |
| RS-006 | Subagent nesting & worktrees | 2026-06-04 | Complete | Nested dispatch blocked (subagents have no dispatch tool); subagent worktree create/commit and sequential merge with abort-redo fallback all proven — evidence selects orchestrator-owned fan-out (one level deep), not nested |
| RS-007 | Multi-workstream SDD | 2026-07-21 | Complete | Plain table append conflicts under concurrency (proven) — use sorted-into-distinct-regions or per-ws-owned rows/files; milestone staleness generalizes to ws-scope for plan/implement/replan, new branches for verify/specs; v3→v4 migrate is copy-verify-flip-cleanup with .sdd-version flipped last; ws-prefixed IDs break 4 ID-generators + 1 review convention string, nothing else |
| RS-008 | Harness hardening | 2026-09-17 | Complete | Loop-control state needs no new artifact (per-session fix count, replan count derived from plan-history, checkpoint in the plan's blocked-task note); chunk-close verifier is a second executor of sdd-implement Step 4, never sdd-review; structured RETURN block + fixed-shape repair packet; lint can check all marker contracts via REQUIRED rows, needs 3 small code changes (size warn, backtick refs, remediation); write-scope check = porcelain delta + committed delta + ancestry — all 12 in-scope ideas research-complete |
| RS-HARNESSP2-001 | Harness hardening, part 2 | 2026-09-17 | Complete | Telemetry fits as a gitignored root-level `.sdd/telemetry.jsonl` transcript of v5 gate signals, orchestrator-appended, read by no phase-detection input (REQ-HARN-027 needs an amendment); Red/Blue is a read-only opt-in third dispatch kind at verify with a `RED_VERDICT:` token, never sdd-review; arbitration is a session-local set comparison over per-round C/M lines rendering a `REVIEW: CONTRADICTION` pause; `sdd-gc` sweep table delivered (a third checkable now via lint, most others small stdlib code, two not mechanical) with DONE/entry cadence and fix tasks parked in verification.md §Next Steps; multi-run evaluation is a no-go as a harness this cycle (telemetry first, REQ-ORCH-011 evaluation-mode exception, orchestrator-only outer loop) but a go as requirement + scorer design + manual N=3 pilot; probe 2 surfaces a catch-up-merge false-positive class, probe 1 awaits implement |
| RS-HARNESSP3-001 | Harness hardening, part 3 | 2026-09-18 | Complete | Write-scope fidelity closed by a bounded (path, sha) fourth observation over the already-dirty set only — probe reproduced the blindness and the fix, stash-create and temp-index alternatives rejected on measured cost; return drift tracks whether the literal key block sits inside the fenced template body or outside it (verifier and red are outside), and only a malformed budget_consumed shape earns a pause; arbitration's W_N must union the fix's writes with any deliverable regeneration since round N, resolved at section level so nothing is weakened; telemetry assurance is one positive gate line carrying the session append counter, no file read; red follow-ups — map breaks from the location red already returns, derive new-ground-vs-regression by re-running the prior round's reproduce command, and let the Verified cell read pending-red with the existing flip; cycle identity needs a research_id stamp on verification.md and plan.md (neither carries one today), requirements status deliberately exempt; both pilot corrections confirmed, the aggregate-regeneration one larger than expected; Q8 closes at 4 in, 3 out |
| RS-HARNESSP4-001 | Harness hardening, part 4 | 2026-09-18 | Complete | `COMMIT:` needs two members and one two-sha comparand (`git diff --name-only HEAD_gate HEAD_landed`), not V14's `git show HEAD` — probe showed the latter names only the last leaf commit on a fast-forward and nothing on a merge commit; a clean merge never drops a path (only abort-and-redo re-derives), so no third member; the signal is post-decision in sequential mode and after `SCOPE:` only at the fan-out per-leaf gate (9 text + 2 code files, from a reproducible 17-file `SCOPE:` grep set); telemetry `expected` should be derived from cross-field implications (a `chunk_verdict`/`review_verdict`/`redo`/`loop-back-to-fix` on a record proves its sibling dispatch ran) — the frozen p3 fixture implies 11 verifiers, 3 first attempts, 5 stage reviews and 2 mis-typed fixes recorded 0 times (39 expected vs 20 records), plus five domain/key-set violation classes for `--lint`; git-commit counting rejected (heads null on every implement record, 20 commits for 8 chunks), plan chunk count accepted only as an opt-in floor |
| RS-HARNESSP5-001 | Harness hardening, part 5 | 2026-09-19 | Complete | Regen provenance stays diff-based — the p4 pause on unchanged sections of a byte-identical regeneration was a true class (b) positive, and a three-scenario offline fixture (A1 p4 case / A2 B8 case / A3 untouched-file control) in the scope self-test discriminates the readings and closes both ARB rows without a live run; the six telemetry findings close with one writer rule (a stage-level fix carries no `chunk_verdict`), a `v: 2`-only equal-heads rule, and four one-condition reader fixes on a new frozen p4 fixture, no schema change; `plan.md` `status: complete` is flipped by the orchestrator at the implement stage gate `proceed` in its own bookkeeping commit (outside the `COMMIT:` range), never by a leaf or by sdd-verify |
| RS-HARNESSP6-001 | Harness hardening, part 6 (terminal) | 2026-09-20 | Complete | Shared-spec staleness is a structural false positive — the 44 warnings fan out from just three (spec, category-file) pairs and the `requires:`-scoped option is already implemented and retires zero, so fold the per-id fan-out into one finding per pair (41 retired, zero false negatives) and carry the folded class at `info`; git-state mutation by a read-only leaf is invisible because the porcelain delta is one-directional, closed by a stash-count/branch/ORIG_HEAD check plus a reverse porcelain delta with the committed delta subtracted, rendered as a `GIT_STATE` line inside the existing `SCOPE:` block (no new token); the Q-IMPL fence asymmetry needs no countability rule at all — all 82 definition ids have an unfenced definition, 0 are fence-only, and routing the definition scan through `visible_lines()` produces byte-identical output, so no marker, tag or allowlist is required; L2 ships as co-located convergence over the existing arbitration `(file, section)` key at section granularity, session-scoped ledger evaluated at every gate, rendered as an informational `CONVERGENCE:` token at position 6c that never pauses — honest floor stated at the time as 2-of-3 recall against the harness-p3 origin case — **superseded 2026-09-20**: the Chunk 8 resolving spike measured the co-located key at zero clusters over three cycles and none of three on that origin case, and L2 shipped at a descoped floor (shared id primary, sectionless-file rule, `(file, section)` retained but demoted with no recall claim), since conceptual convergence needs a root-cause field or a fifth layer, both excluded; deferral sweep dispositions all 5 §Out of Scope entries plus 5 inherited harness-p5 §Next Steps items, and names the 3 new settled exclusions to record |
| RS-MARKETPLACE-001 | Marketplace release | 2026-09-21 | Complete | `docs/` never feeds phase detection — all 796 docs citations in skills/ are cwd-relative (0 non-relative) and `sdd-gc.py` roots on the operator's git toplevel (not its own repo, correcting the carried-forward warning), so `docs/` stays in the repository and out of the plugin's component list; of 1206 `tools/sdd-*.py` references (432 executable / 774 prose) only the 15 executable ones inside skills/ break, fixed by bundling `sdd-gc.py` and `sdd-telemetry.py` skill-dir-relative with an explicit `--root` since `${CLAUDE_PLUGIN_ROOT}` expands only in manifest fields and commands, never a skill body; one umbrella `sdd` plugin with `"source": "./"` and an explicit component list (a split is rejected on 263 name-keyed cross-skill references and moves zero files); rename first then scaffold — 1841 name refs / 558 path-shaped over 91 files plus 91 keyed lines of `sdd-skill-lint.py`, whose exit 0 is the verifiable intermediate state at zero double-touch cost; agents ARE dispatchable (seven installed `sui-ai` agent files load as seven `<plugin>:<agent>` subagent types), and a sweep of n=32 first-party agent files gives the contract as `name`+`description` required (32/32) with `tools` (23/32, comma string **or** YAML flow sequence) and `model` (24/32) optional — `color` is a real first-party field (20/32 across five official plugins) and is RETAINED in `CLAUDE.md`, only `emoji`/`vibe` (0/32) are this repo's invention and candidates to drop; cited by subagent_type name plus a parenthetical lint-checkable file path |
| RS-PACKAGING-001 | Packaging follow-up | 2026-09-21 | Complete | No exclusion filter exists — a `git-subdir` source plus the authorized `plugins/sdd/` root move is the only lever, costed at 45 files moved / 154 staying; the linter then needs TWO roots, not one prefix, because its retired-prefix scope and template-pair spec side key on paths that stay; every suite-specific row is flat data needing no code and `tools/gc.py` carries none of them; the cheapest non-circular fixture is a `git archive <sha>` pipe into `tar -x`, which `diff`s empty against the live install — **rejected at its stage gate 2026-09-21 and superseded by RS-PACKAGING-002**: the install mechanism was read, not executed, and two of its own answers contradicted each other about which root the sweep runs against; its carried findings (45/154/199, the two-root need, `git-subdir` as the only lever) survive as inputs to 002 |
| RS-PACKAGING-002 | Root interface (git-subdir + sweep roots) | 2026-09-21 | Complete | `git-subdir` proven by a real install: the materialised tree holds exactly the plugin subdirectory's three files with all four root-level files absent, a same-repository marketplace entry resolves (observed over a `file://` url, not a GitHub remote — the GitHub-hosted shape is inferred, not executed), and declared plugin-root-relative component paths resolve — so `marketplace.json` costs one field, not 14 component edits; conventional discovery from the plugin root happens regardless of what the entry declares, and a declaration naming an absent path neither subtracts from nor redirects that inventory (the stronger claim that the declared arrays are never consulted is NOT established — the discriminating control, a real component at a non-conventional path, was never run). Q2's premise is corrected: `tools/skill-lint.rules.json` does not exist, the 56 suite-gated rows (REQUIRED 40 + VERSION_GATED_SKILLS 9 + V4_CONTRACT_SKILLS 7) are in-code constants already gated by `suite_rules` while FORBIDDEN's 13 rows are ungated and have no path key, so option (B) — `suite_root` defaulting to the script's plugin root, `corpus_root` taking over the positional argument — is recommended at zero new artifacts, leaving 45/154/199 intact. Two acceptance-vector parts (`FILES_SWEPT`, the two-distinct-root fixture) arrive PROVISIONAL and part 1 is blocked on a `--print-population` task — see §Open Questions |
| RS-PACKAGING-003 | The four open decisions (capped) | 2026-09-21 | Complete | **Depends on RS-PACKAGING-002 as authoritative evidence — it is an input, not a superseded predecessor.** All four decisions closed under a 300-line cap, each with a confidence word: (D1, high) the 56 suite rows retarget to the suite root — their consumer-tree failures were loud about the wrong tree — carried by a two-root **constructor parameter**, with the `--suite-root` CLI surface deferred to requirements and `--no-suite-rules` rejected outright; the amended F11 target carries two exceptions, the ungated-but-suite-bound retired-prefix rule and the gated-but-corpus-bound `TEMPLATE_PAIRS` spec side; (D2, medium) both dual-rooted checks bind per entry — `skills`/`tools`/`agents` to the suite root, `docs/spec`/`docs/requirements` to the corpus root, `.claude-plugin` and the scope files to the deduplicated union (five names once `Q-IMPL-MARKETPLACE-017` drops `README.org`), `TEMPLATE_PAIRS` source to the suite root and `spec` to the corpus root with the spec side skipped under disjoint roots — a wrong binding caught by a set-membership assertion over both manifest paths, not a file count; (D3, medium) no absolute corpus count is sound — a live duplicate-freeness construction guard reporting a `fail`-severity finding plus an exact fixture-local count, with live zero-sweep detection deliberately given up — **D3 partially superseded by RS-CONSUMERGEOMETRY-001**: the count argument stands, but zero-sweep detection is available via a non-count signal (the conditioned floor reads its condition through the binding under test) and the `FILES_SWEPT=<n>` output line left informational; (D4, low) three geometries — A nested (shipping shape, per-root path rendering), B disjoint (table-row violation present, walk-class violation specifically absent), and C equal roots, the only geometry where a file is reachable from both terms and so the only one that pins single-sweep — with the containment-narrowing formulation declined as a shipping-semantics change. Sequencing: a `--print-population` task precedes the headline 40-row criterion, and the carried `skills/verify/SKILL.md:169` repair follows D1/D2's root bindings. |
| RS-CONSUMERGEOMETRY-001 | Consumer geometry (the disjoint suite root) | 2026-09-21 | Complete | **Depends on RS-PACKAGING-003 as input, superseding only its D3** — every one of the linter's eight checks reduces under a disjoint suite root, in three classes: A, `check_structure()` emits one finding and returns; B, `check_forbidden`/`check_ordinals`/`check_links`/`check_size` pass vacuously over a `walk()` of 0 files and print `OK: 0 file(s) clean`; C, `check_required` (all 56 gated rows), `check_template_drift` and `check_retired_prefix` (36 of 112 scope entries rebind) run against the installed cache instead of the working tree. `gc.py` has no suite-root binding of its own but *constructs* the geometry for every consumer (`lint_command()` passes the corpus root only) and its `lint_path()` fallback candidate is dead post-move. The decisive mechanism is that `walk()` joins `<root>/skills` per swept root and `<repo>/skills/` does not exist, so a union admitting the disjoint suite root would walk the **cache's** 25 files, not the working tree's — tool-side widening is therefore **rejected** (it converts a loud reduction into a silent wrong-tree pass) and the recommendation is a per-geometry split: an explicit suite root (D1's deferred `--suite-root`, now load-bearing) for the operator's own geometry, today's union unchanged for a foreign consumer, plus a falsifiable `GEOMETRY:` token and a `— NOTHING SWEPT` summary suffix in both. Rebinding the 56 rows to the union is withdrawn: 16 of them are pinned to the suite root by the C12.1 self-test fixture and a spec clause, leaving a 40-row-only proposal for specs. REQ-PKG-MARKETPLACE-007 does not bind — it never named the linter at all, and its `git diff` comparand is un-re-runnable since the `tools/` move; a gate **can** observe the geometry (the scratch-root technique is already in `--self-test` and already pre-commit-gated), with five named pinning gaps (a)–(e) left; and `skills/orchestrate/tools/` is reachable by no working invocation (the bundled `gc.py` exits on linter-missing) — remove it, but only after amending REQ-PKG-MARKETPLACE-006, a six-record edit that includes one shipped plugin file |
| RS-PIPELINEOBSERVABILITY-001 | Harness self-verification (pipeline observability) | 2026-09-22 | Complete | Telemetry reader drops 130 `v`-less records (53 consumer-geometry, 77 packaging) that `rec 39` counted as written — `append` must validate through the existing `lint_records` before the counter renders, 36 of 53 migratable and 17 declared lost; `GIT_STATE` detection exists and fired twice (kickoff corrected) but no agent body names git state and a violating verifier's verdict is still consumed, hook enforcement OPEN; replaying the 17 review rounds under consecutive-`REJECT` counting with tier conversion (V2, fully specified with a `ROUND_MAX` backstop) fires the cap 2× instead of 4× and forces 3 interventions instead of 4 (no round total is claimed — requirements binds V2 rules 1–4 and the `ROUND_MAX` backstop only) — V2's saving is correctness, not review calls — so the post-manual review rule is load-bearing, on correctness not round count; tier-heading parsing would have caught 3 rounds, operationalising the return contract's existing disagrees-with-prose `REVIEW: MALFORMED` pause for `APPROVE` and `APPROVE_WITH_FIXES` alike; the two class-(b) pauses were fix-induced ground, decidable by heading existence at round N's sha; `literal-anchor` measures 69 anchors in 7 files (warn, folded; scope bound to spec + requirements, ws and research records out by decision), `self-matching-grep` 4 true positives, 0 false (fail, corpus repaired in the same commit); gaps 8–11 are one-liners with falsifiers, gap 7 is not (30 legacy bare definitions); every recommended change is mapped to the requirement ids it amends or leaves intact (15 rows, each with a mechanical reversion pin or marked prose-only, 3 decisions handed to requirements); all eleven gaps proposed, four sub-items excluded. |
